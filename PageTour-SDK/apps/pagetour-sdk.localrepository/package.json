{
  "name": "@tai-sdk/pagetour.localrepository",
  "version": "0.0.1",
  "description": "Local storage repository plugin for pagetour sdk",
  "repository": {
    "type": "git"
  },
  "main": "dist/LocalStorageRepository.js",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist",
    "watch": "tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "files": [
    "dist/*"
  ],
  "license": "MIT",
  "dependencies": {
    "@tai-sdk/pagetour": "0.0.1",
    "uuid": "^8.3.2"
  },
  "peerDependencies": {
    "@tai-sdk/pagetour": "^0.0.1"
  }
}
